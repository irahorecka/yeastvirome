% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_virome_species.R
\name{summarise_virome_species}
\alias{summarise_virome_species}
\title{Summarise virome counts by viral \emph{species}}
\usage{
summarise_virome_species(virome_df, vmeta_df)
}
\arguments{
\item{virome_df}{A wide tibble or data frame; first column \code{std_name},
remaining numeric columns store abundance (e.g. RPM) for each viral contig.}

\item{vmeta_df}{Metadata tibble with columns \code{import_label} (contig id)
and \code{species}.}
}
\value{
A wide tibble with \code{std_name} + one column per species.
}
\description{
Converts a wide \code{virome_df} (one row per strain, columns = contigs)
into a wide table where each viral species is a column containing the sum
of all contig abundances that belong to that species.
}
\examples{
\dontrun{
species_tbl <- summarise_virome_species(virome_df, vmeta_df)
}
}
